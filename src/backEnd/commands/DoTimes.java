package backEnd.commands;

import Controller.Control;
import backEnd.commands.Command;

public class DoTimes extends Command {

  public DoTimes(String[] varargs, Control control) {
    super(varargs, control);
  }
}
